import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class ThemeColor {
  static const defaultTheme = Color.fromRGBO(135, 5, 190, 1);
  static const backgroundTheme = Colors.white;
  static const widgetBackgroundTheme = Color.fromRGBO(246, 246, 246, 1);
  static const widgetForegroundTheme = Color.fromRGBO(189, 195, 202, 1);
}
